{
  "name": "gitintel",
  "private": true,
  "version": "0.1.0",
  "description": "AI-powered developer intelligence platform for GitHub profiles.",
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "typecheck": "npm run typecheck --workspace frontend"
  },
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
